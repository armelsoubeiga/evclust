[tool.poetry]
name = "evclust"
version = "0.1"
description = "Evidential c-Means Clustering"
authors = ["Armel SOUBEIGA"]
license = "LICENSE"
readme = "README.md"

[tool.poetry.dependencies]
python = "3.9"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
